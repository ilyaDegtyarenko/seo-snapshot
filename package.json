{
  "name": "seo-snapshot",
  "version": "0.1.0",
  "description": "Small CLI project for crawling pages and generating practical SEO audit snapshots.",
  "type": "module",
  "bin": {
    "seo-snapshot": "./bin/seo-snapshot.mjs"
  },
  "scripts": {
    "snapshot": "node --env-file-if-exists .env ./bin/seo-snapshot.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.9"
  },
  "keywords": [
    "seo",
    "audit",
    "snapshot",
    "crawler",
    "cli"
  ],
  "license": "MIT",
  "dependencies": {
    "fflate": "^0.8.2",
    "html-minifier-terser": "^7.2.0",
    "parse5": "^8.0.1"
  }
}
